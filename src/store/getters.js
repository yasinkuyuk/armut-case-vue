const getters = {
  currentIndex(state) {
    return state.currentIndex;
  },
  questions(state) {
    return state.questions;
  },
  service(state) {
    return state.service;
  },
};

export default getters;
